# Copy to packages/database/.env
# Prisma CLI (migrate / seed / generate) reads DATABASE_URL from this file
# because those commands run with packages/database as the working directory.
# Keep it in sync with apps/api/.env — same database.
DATABASE_URL=postgresql://volttackle:volttackle@localhost:15432/volttackle?schema=public
